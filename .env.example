# Copy this file to .env and fill in values for the target environment.
# Never commit .env to git.

# Supabase project — different per environment (production / Gabe's clone / etc.)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional: surfaces in /feedback panel; defaults to 1.0.0 if unset.
VITE_APP_VERSION=1.0.0
