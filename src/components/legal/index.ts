/**
 * Barrel kept at the Inkfleet path (`src/components/legal`) so the module can
 * be lifted back into Inkfleet without rewriting imports. STORM keeps the
 * implementation with the rest of the module under `src/modules/legal`.
 */
export * from '@/modules/legal/components';
