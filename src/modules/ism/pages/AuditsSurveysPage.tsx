import React from 'react';
import Audits from '@/modules/audits/pages/Audits';

// ISM Audits & Surveys page - uses the main Audits page component
// This provides the same functionality within the ISM navigation context
const AuditsSurveysPage: React.FC = () => {
  return <Audits />;
};

export default AuditsSurveysPage;
